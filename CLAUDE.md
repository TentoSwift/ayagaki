# CLAUDE.md — 綾書エディタ（Ayagaki）

組紐（高台）の綾書を設計するアプリ。Web 版 + macOS/iOS/iPadOS ネイティブ + MCP サーバ内蔵。
**組み方はデザインごとに選ぶ**（`braidType`: "yasuda"=二枚安田組・既定 / "korai"=二枚高麗組）。
題材はユーザー所有の組紐書籍（安田組 4-4〜4-16、高麗組 1-3〜1-15、60玉/68玉）。**書籍との一致が正**。

## 構成

```
index.html              Web 版（単一ファイル・依存なし）。GitHub Pages: https://tentoswift.github.io/ayagaki/
apple/                  ネイティブアプリ（XcodeGen 管理）
  project.yml           ターゲット: Ayagaki-iOS (iOS16+) / Ayagaki-macOS (macOS13+)
  Shared/               ソース共有。BraidModel.swift が仕様の中心
  Shared/MCP/           MCP サーバ（HTTP + stdio）
docs/notation-reference.md  書籍 4-15「四角」の転記（正解データ）と手取り機構（4-4〜4-8）の要約
docs/korai-reference.md     二枚高麗組（1-4〜1-15）の転記
scripts/test-notation.py    安田組の記号生成の回帰テスト（4-15 全4例と突き合わせ）
scripts/test-korai.py       高麗組の綾名の回帰テスト（1-3「手取り練習」の42段と突き合わせ）
```

回帰テストは既定で `/Applications/Ayagaki.app` を使う。別のビルドを検証するときは
`AYAGAKI_BIN=apple/macbuild/Build/Products/Release/Ayagaki.app/Contents/MacOS/Ayagaki python3 scripts/test-korai.py`。

## ビルド・デプロイ（macOS）

```bash
cd apple && xcodegen generate   # 初回・project.yml 変更時
xcodebuild build -project Ayagaki.xcodeproj -scheme Ayagaki-macOS -configuration Release \
  -destination 'platform=macOS,arch=arm64' -allowProvisioningUpdates -derivedDataPath macbuild
# /Applications へ設置（アプリ終了→差し替え→起動）
pkill -f "Ayagaki.app/Contents/MacOS/Ayagaki"; rm -rf /Applications/Ayagaki.app
ditto macbuild/Build/Products/Release/Ayagaki.app /Applications/Ayagaki.app
open /Applications/Ayagaki.app
# 回帰テスト（要 /Applications/Ayagaki.app）
python3 scripts/test-notation.py
```

- チームID LV3H7Q68W6、バンドルID com.tento.ayagaki、iCloud コンテナ iCloud.com.tento.ayagaki
- デザインは Core Data + NSPersistentCloudKitContainer で **iCloud 同期**（同じ Apple ID の Mac/iOS 間で共有される）
- ⚠️ シミュレータで `CODE_SIGNING_ALLOWED=NO` にすると CloudKit 初期化がクラッシュ（署名ありでビルドする）

## MCP（Claude / Codex からデザイン操作）

- 登録（stdio・アプリ起動不要）: `claude mcp add ayagaki -- /Applications/Ayagaki.app/Contents/MacOS/Ayagaki --mcp`
  / Codex: `codex mcp add ayagaki -- /Applications/Ayagaki.app/Contents/MacOS/Ayagaki --mcp`
- アプリ起動中は HTTP も可: `http://127.0.0.1:53536/mcp`
- ツール10個: list/get/create/update_design, paint, set_cells, get_notation, export_pdf, clear_cells, delete_design
- ⚠️ stdio で viewContext（main キュー + dispatchMain）に保存するとデッドロック → 専用バックグラウンドコンテキスト + DesignStore の performAndWait で解決済み。⚠️ SwiftUI ImageRenderer はヘッドレスで永久ハング → PDF は PDFSheetRenderer（CoreGraphics 直描き）

## 仕様（書籍と照合済みの規則）

- グリッド: 片面目数 × 段数。目盛りは **1=中央 … 外端**（綾書定規 4-10/4-11）。**玉数は 60/68/76/84/92/100 に対応**（8玉ごとに片面+2目: 60=13目, 68=15, 76=17, 84=19, 92=21, 100=23。`BraidSpec.cols(forTama:)` と Web の `TAMA_TO_COLS`）。丸数字は ①〜⑳（U+2460）＋㉑〜㉟（U+3251）で23目まで対応。ユーザー確認 2026-07-11
- **地の色に対して柄色は1つ**（ユーザー確認 2026-07-07）: セル値は 0=地・1=柄のみ。パレット UI は2色表示、MCP の入力検証も 0〜1（保存データの配列は互換のため4色のまま）
- 右半面は d=0 が中央線上で**右段が先に始まり**、左半面が1目(c)下がる配置で左右が隙間なく連続（左右1段ずれ。ユーザー確認 2026-07-07）
- 記号 = [糸交換の数字] + [綾名] + [⬆]：
  - **綾名**: 1目おきの「入れかえの目」（d=1,3,5…、60玉=6目）だけを読む。**全区間を「上a下b…」で数字付きで略さず書く**（1も書く。合計は常に6。例: 上1下5、下3上1下2。手書きの綾書の書式・ユーザー確認 2026-07-07）
  - **数字の書式**: 糸交換（数字）と戻し（丸数字）は1つの数列に数値順でまとめ、通常数字の後にだけ「.」（例: ②3.4.⑩⑪）。同種のみなら範囲圧縮（数字6個以上「2〜11.」、丸数字4個以上「②〜⑩」）
  - **戻し**: 戻し（丸数字）が要るのは**糸交換のみ**。上の操作（綾の手取りで目を上げる）には戻しを付けない（ユーザー確認 2026-07-07）。飛びの糸交換は**+2の番号**（2→④、3→⑤…）で模様の終わりの1段先にまとめて戻す。手取りの⬆のみ（中央の色なし）のブロックは書籍 4-15 の一括の戻し「②〜(M+2)」。数字は片面の目数で頭打ち、3個以下は並記・4個以上は範囲形式。**丸数字は終わった段の直後ではなく、ひとつ段を飛ばした先に表示**（次の模様と重なる場合はその記号の前に付く。回帰テストの 4-15 期待値もこの1段飛ばしで書いてある）。**中央の糸交換（⬆）の戻しは③**（毎回同じ番号）で、**反対の半面に出る**（左の⬆ → 右の記号列に③）。**中央の色から45度（1段1目でも、1段おきの2段2目でも続き扱い）に色が続く場合は、途中に糸交換を書かず、戻しは色が地色になる所の2段先**。番号は**終端の位置の番号+2（end.d+3）**: 中央単独=③、d2で終われば⑤、d4=⑦（進み方・色数によらず終端の位置で決まる。偶数列の糸交換の戻しも同じ式。ユーザー確認 2026-07-07）。**一番端（最外の目 d=12/14）まで達した連なりには戻しを書かない**（端で折り返すため。ユーザー確認 2026-07-07）
  - **縦に進む列（偶数列 d=2,4,…）は糸交換のみ**（綾名には入れない。ユーザー確認 2026-07-07）。番号は中央=1 からの通し（d+1: 3,5,7…13）。色のある段にその番号を書く。**戻しは、交換した対が45度で進んで色が地色になった後、終端から45°に進んだ「次の偶数列（交換できる目）」の段に、その位置の番号で行う**: 終端が偶数列なら2段先に番号+3、奇数列なら1段先に番号+2（例: d2止まり→2段先に⑤、d5止まり→1段先に⑦。ユーザー確認 2026-07-07）。**一番端の列でも糸戻しは行う。ただし着地する列の対が別の糸交換で使われている（その行範囲で発火した色がある）場合は戻し無し。端を越える着地も無し（端の列の交換自体の戻しは書けない）**（例: 60玉で d10・d12 を同段に塗ると d10 の戻し⑬は無し／d10 単独なら端 d12 に着地して⑬を書く。ユーザー確認 2026-07-11）。**45°の辿りは歩調を一貫させる**: 1段1目で進んできた連なりは空きを2段2目で飛び越えない（逆も同じ。歩調が変わる所で連なりは切れ、そこで戻す。ユーザー確認 2026-07-07）。**糸交換・戻しの番号は片面の目数（60玉=13）で頭打ち**
  - **斜めに色が続くときは色が変わるところだけ糸交換**（ユーザー確認 2026-07-07）: 前段の±1目から移ってきた色の目は交換に数えない。飛びの判定（2目以上増）も、縦列（偶数列）の毎段の数字も、斜めから繋がらない新しい目だけが対象
  - **糸交換から45度で続く色には、地色に戻る所まで糸交換を書かない**（ユーザー確認 2026-07-07）: 縞の一番先の交換も、遡って糸交換の目（中央、または fired の偶数列＝途中で発火した糸交換）に行き着く縞なら書かない（その区間は戻しが受け持つ。綾の段に糸交換番号は付けない。ユーザー確認 2026-07-07）。連なりの由来は追跡で判定（carried/fired）: **中央または発火した偶数列から続く色だけが省略対象**で、**綾（奇数列）から続いただけの偶数列には糸交換を書く**（例: d=11 の綾の次の段の d=12 →「13.」。ユーザー確認 2026-07-07）。**連なりの続き扱いは外向き（1目外・1段おきの2目外）のみ**で、内向きの斜めから続いた偶数列は糸交換を書く（そこから新しい連なりが始まり、終端で戻す。ユーザー確認 2026-07-07）
  - **斜めに進んだ縞は一番先で糸交換**（ユーザー確認 2026-07-07）: 入れかえの目を内側から外へ移ってきた色の縞は、一番先の目の色が終わる段に、その場所の番号（d+1）を1回だけ書く。戻しは不要。判定＝内側（d-2、d=1は中央の上ル）から移ってきて（直前の段または2段前）、外側（d+2）へは続かず（b+2段先まで見る）、かつ内側が縞の始まる前から終端まで塗られたままではない（＝広がる4-15型ではない）run。1段おきに塗る斜めにも対応
  - **飛び**: 入れかえの目が一度に2目以上増える段に「2.3.」等を前置。数字6個以上は「2〜M.」に範囲圧縮（書籍 4-16）。数字・丸数字と綾名の区切りはピリオド（ナミの前は区切りなし）
  - **⬆（中央で上ル）**: C 配列が唯一の情報源（index 2r=右半面・2r+1=左半面）。中央の2列（d=0）に色を置くと自動で**反対側の半面**に⬆。**左半面の⬆の表示は中央の色の1段前**（ユーザー確認 2026-07-07）。ただし**記号（戻し・縞・橋渡し）の法則は左右同一**で、上ルの効く段は左右とも中央の色と同じ段（ユーザー確認 2026-07-07: ずらすのは⬆の表示のみ）。連続 k 段の⬆が終わった段に ①〜ⓚ の戻し（置いた段に数字は出さない）。ナミ⬆の段は数えに中立。⬆ は中央の色（d=0）からの自動導出と MCP の C 指定のみ（記号表は読み取り専用。ユーザー確認 2026-07-11）
- **手取り図（書籍 4-4〜4-9）**: 記号表の段を選ぶと、その段の綾名から糸のすくい方の図を自動生成（アプリ=`TedoriView.swift`、Web=`tedoriSVG()`）。片腕の糸数 N=玉数/4（60玉=15）。
  - 帯は左から A・B（左の対）／間を空けて C・D（右の対）。玉数は **図1: N-1, N, N+1, N ／ 図2: N-1, N+1, N, N**（写真 h_nami1/h_nami2/h_ue1_1/h_ue1_2 実測）。段は図1・図2 とも N+1 段で、図1 は C=1〜N+1・D=2〜N+1・B=2〜N+1・A=3〜N+1、図2 は C/D=1〜N・B=1〜N+1・A=2〜N
  - 動かす糸は C の一番上の黒玉の左にフックしてから C・D の間を下り、**スロット段（図1: 5,7,…,N ／ 図2: 4,6,…,N-1）**で「下＝黒玉(C)の左を回る／上＝白玉(D)の右を回る」。間の段は素通り。最後の段を素通りしてから左へ矢印
  - **線の作り方（写真 h_nami1 実測）**: 通過点の折れ線をならすのではなく、**幾何的に組み立てて接線連続**にする（`TedoriLayout.threadSegments` / `tdThreadPath`。円弧・楕円弧もすべて3次ベジェに落として Swift と Web で同じ形）。素通りは xGap の**縦一直線**、ループは玉の中心を回る**半楕円**（横 `loopA`=玉の半径+0.34、縦 `loopB`）、その前後を**半径 `turnR`=xGap−xC の四分円**でつなぐ。`loopB + turnR = 1段` にしてあるので、ループの1段前と1段後はちょうど (xGap, 段) を真下向きに通り、隣り合うループは自然な S 字で、上→下・下→上も同じ手順でつながる。図1 のフックは同じ半楕円の一部（`hookOffset` だけ下げた中心・上端 t=0.55 から）、図2 は D の一番上の白玉の左から S 字で入って C の一番上の黒玉を1段目のループとして回る（写真 h_nami2）。下端は最後の段から矢印の始点へ、矢印と同じ向きの接線で大きく曲がる
  - **スロットの並びは綾名の単位の逆順**（上1下5 → 下下下下下上）。**図2 は上下を入れかえた綾名**＝各単位を反転（`Notation.tedoriSlots` / `tedoriSlots()`）。ナミは図1 が全「下」・図2 が全「上」。左半面（図3・図4）は左右鏡像
  - 綾名の単位は文字列を再パースせず `Notation.ayaSlots` / `ayaSlots()` から取る（記号の生成規則と常に同じ材料）
  - **全段表示**: 交換記号カードの「記号表／手取り図」切り替えで、全段を上から順に並べた手順書表示になる（同じ記号・同じ塗りが続く段はまとめて「4〜6段」。図をタップ/クリックでその段をグリッド上でハイライト）。「記号表」モードは表＋選んだ段のパネル（`TedoriListView` / Web の `tedoriGroupsList()`）
  - **下端の交差（渡る糸と相手の糸）**: 各図の下端で、動かした糸は矢印で反対側の対（図1 なら左の対 B、図2 なら A）へ渡り、反対の対から渡ってくる相手の糸（細い灰色の逆向き矢印）と左右の対の中間で交差する。**⬆ の付いた段は、その半面の最初の手順（図1・図3）だけ渡る糸が相手の糸の上を通る（上ル）。2番目の手順（図2・図4）と ⬆ の無い段は下を通る（下ル）**（ユーザー確認 2026-09-09: 「図3だけ上がり、図4は下がる」。右半面も同様に図1のみと解釈）。上ルの図だけ相手の糸（灰）と交差（相手の線を途切れさせる）と「上ル」のラベルを描き、操作の無い図（下ル）は書籍どおり動かした糸の矢印のみで相手の糸もラベルも描かない（ユーザー確認 2026-09-10）。⬆ の有無はその半面の記号から取る（`RowSymbol.rise` / Web の `sideSymbols()` の `rise`。記号文字列の出力は不変）。ユーザー確認 2026-09-09
  - **糸交換の印（書籍 4-8 下段 A/B）**: 記号に糸交換の数字や戻しの丸数字がある半面には、図1・図2 の横に帯と玉だけの図を1枚足し、C と D の対応する玉の間に両矢印と番号（戻しは丸数字）を描く。位置は**手前（下）から数えた番号そのまま**（一番下=1。番号は片面の目数で頭打ち）。番号は記号文字列を再パースせず `Notation.sideSymbols`（Web は `sideSymbols()`）の `plain`／`circled` から取る（`sideTexts` の出力は不変）
- 変更したら必ず `python3 scripts/test-notation.py` で 4-15 と一致することを確認

## 二枚高麗組（杉綾＝ヘリンボーンの目）

デザインごとに `braidType: "korai"` を選ぶと、グリッド・記号生成が高麗組に切り替わる。
安田組の生成コード（`Notation.sideSymbols` / Web の `sideSymbols()`）には一切触れていない。

- **目の格子**（書籍 1-3 の 42 段で検証済み）: 目は 45° に傾いた 2:1 の長方形（短辺 s = c√2、長辺 2s。c = 畝の間隔の半分）。
  各半面は縦の畝（wale）が **(cols+1)/2 本**（60玉＝片面13目なら 7 本）。中央側から w=0..6。
  同じ畝の目は縦に積み上がる（ピッチ 2c）。目の中心は
  `x = cx ± (1+2w)c`、`y = y0 + (k−1)·2c − (w−1)·2c + 半段`（右半面は w 偶数、**左半面は w 奇数**のとき +c）。
  外側の畝ほど早い段の高さになるので**上端が自然に ∨ になる**。左半面の半段のずれを右と逆にしないと
  中央の継ぎ目で目が重なる。幾何: `KoraiGeometry` / Web の `koraiGeo()`
- **向き**: 右半面は w 奇数（1,3,5）が ／＝綾（手取り）の目、偶数（0,2,4,6）が ＼＝糸交換の目
  （w=0 が中央の目「1」、w=2 が 3.4.5、w=4 が 7.8.9、w=6 が 11.12.13＝端の目）。左半面は鏡像
- **データの持ち方**: 既存の `CellGrid` をそのまま使い、**畝 w の目 k を L/R[k−1][2w]** に入れる
  （奇数 d は未使用。MCP の paint では pos = 2w+1）。C 配列は高麗組では使わない
  （保存・iCloud・MCP の 0/1 検証はそのまま流用できる）
- **綾名**: 段 r は畝 w=1,3,5 の目を **k=r** で読む（色付き＝上・白＝下、内側から3つ）。
  走りが2つなら数字付き（上1下2・上2下1・下1上2・下2上1）、3つなら数字なし（上下上・下上下）、
  全部上なら 上3、全部下なら ナミ。`Notation.koraiSideSymbols` / Web の `koraiSideSymbols()`
- **糸の動きのモデル**: 1 本の糸は 1 段に 1 目盛ずつ外へ進む。段 r で位置 n にいる糸を **t = r − n + 1**
  と呼ぶと、糸 t が通る＼の目は **(w, k) = (w, t + 2w)**。すなわち目 (w,k) の一つ手前の＼の目は **(w−2, k−4)**
- **糸交換の ●（数字・丸数字）**: 目 (w,k) の色を一つ手前の目 (w−2, k−4) の色と比べ、
  **白→柄なら素の数字**（下段の柄糸を上段へ上げる）、**柄→白なら丸数字**（上げた糸を下段へ戻す）。
  w=0 の手前は常に白。手前がグリッドの外に出る k<5 は判定できないので印を付けない（`Notation.koraiMarks` / `koraiMarks()`）
- **数字を書く段**: 1 つの ● は 3 段のうちどの段に書いてもよい（入れかえは手取りの綾に隠れる部分で行うので
  3 段先まで入れかえられる）。同じ畝 w の ● を k の小さい順に見て、まだ組に入っていない一番小さい k を k0 とし、
  **k0 ≤ k ≤ k0+2 の ● を 1 組にして段 r = k0 にまとめて書く**。組の中の目 k の番号は **n = 2w+1−(k−k0)**
  （w=2 なら 5,4,3 ／ w=4 なら 9,8,7 ／ w=6 なら 13,12,11）。例外: ● が 3 個連続する並びは必ず 1 組（その先頭は
  別の組に巻き込まない）、k が飛んでいる ● を同じ組にするのは種類（素/丸）が同じときだけ、k0 が段数を超える組は
  書ける一番遅い段まで引き上げる（`Notation.koraiTokens` / `koraiTokens()`）
- **書式**: 数字と丸数字を数値順に 1 列にし、素の数字の後だけ「.」（例「5.上1下2⬆」「⑦⑧⑨上1下2」「③④5.ナミ」
  「5.⑧9.上1下2」）。**高麗組では範囲圧縮をしない**（1 組は最大 3 個。`Notation.koraiNumberList` / `koraiNumberList()`）
- **⬆（中心の入れかえ）**: 中心 (w=0) の ● に対応するが、**反対の半面の中心の目**を見る（左右の定規は半段ずれ）:
  **右半面の段 r ⇔ 左半面の (0, k=r) が柄 ／ 左半面の段 r ⇔ 右半面の (0, k=r+1) が柄**
- 規則の Python 実装（規則の全文つき）は `scripts/korai_rule.py`、書籍からの格子・綾書は `docs/korai-fixtures/`、
  書籍との照合結果は `docs/korai-fixtures/rule_check.md`。Swift / JS はこの Python 実装の忠実な移植
- **未対応**: （下下）（上上）（トリ）、高麗組の手取り図。
  段ごとの記号は安田組と同じ `RowSymbol`（plain / circled / aya / text / rise）で返しているので後から足せる
- 変更したら `AYAGAKI_BIN=... python3 scripts/test-korai.py`（1-3 の 42 段＋ fixture 全段が
  `scripts/korai_rule.py` の出力と完全一致すること。書籍との一致率は参考表示）

## 残タスク・未解決

- 高麗組の（下下）（上上）（トリ）、高麗組の手取り図。糸交換の数字を「どの段に書くか」は 3 段の自由度があり、
  書籍とは 76%（数字だけなら 80%・⬆ だけなら 95%）の一致（`docs/korai-fixtures/rule_check.md`）
- ⬆の完全自動導出と糸番号リスト（「3,5,7,9 上ル」等）: 糸1本ずつの経路追跡（二枚安田の組成シミュレーション）が必要。4-15 が答え合わせデータ
- 部分的な戻し（模様が全消えせず狭まる場合）の丸数字は未対応（現状は全消え段のみ）
- ~~色違い（柄1→柄2）の塗り替えは糸交換の数字に出ない~~ → 柄色を1つに制限したため解消（2026-07-07）

## 進め方の約束

- 決定は「書籍のページ写真 → ユーザー確認 → 実装 → 4-15 回帰テスト」の順
- Web（index.html）とアプリ（Shared/BraidModel.swift ほか）の**両方**に同じ変更を入れる
- デプロイ: push で GitHub Pages（Web）、/Applications へ ditto（アプリ）
