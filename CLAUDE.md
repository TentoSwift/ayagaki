# CLAUDE.md — 綾書エディタ（Ayagaki）

組紐（高台・二枚安田組）の綾書を設計するアプリ。Web 版 + macOS/iOS/iPadOS ネイティブ + MCP サーバ内蔵。
題材はユーザー所有の組紐書籍（4-4〜4-15、60玉/68玉二枚安田組）。**書籍との一致が正**。

## 構成

```
index.html              Web 版（単一ファイル・依存なし）。GitHub Pages: https://tentoswift.github.io/ayagaki/
apple/                  ネイティブアプリ（XcodeGen 管理）
  project.yml           ターゲット: Ayagaki-iOS (iOS16+) / Ayagaki-macOS (macOS13+)
  Shared/               ソース共有。BraidModel.swift が仕様の中心
  Shared/MCP/           MCP サーバ（HTTP + stdio）
docs/notation-reference.md  書籍 4-15「四角」の転記（正解データ）と手取り機構（4-4〜4-8）の要約
scripts/test-notation.py    記号生成の回帰テスト（4-15 全4例と突き合わせ）
```

## ビルド・デプロイ（macOS）

```bash
cd apple && xcodegen generate   # 初回・project.yml 変更時
xcodebuild build -project Ayagaki.xcodeproj -scheme Ayagaki-macOS -configuration Release \
  -destination 'platform=macOS,arch=arm64' -allowProvisioningUpdates -derivedDataPath macbuild
# /Applications へ設置（アプリ終了→差し替え→起動）
pkill -f "Ayagaki.app/Contents/MacOS/Ayagaki"; rm -rf /Applications/Ayagaki.app
ditto macbuild/Build/Products/Release/Ayagaki.app /Applications/Ayagaki.app
open /Applications/Ayagaki.app
# 回帰テスト（要 /Applications/Ayagaki.app）
python3 scripts/test-notation.py
```

- チームID LV3H7Q68W6、バンドルID com.tento.ayagaki、iCloud コンテナ iCloud.com.tento.ayagaki
- デザインは Core Data + NSPersistentCloudKitContainer で **iCloud 同期**（同じ Apple ID の Mac/iOS 間で共有される）
- ⚠️ シミュレータで `CODE_SIGNING_ALLOWED=NO` にすると CloudKit 初期化がクラッシュ（署名ありでビルドする）

## MCP（Claude / Codex からデザイン操作）

- 登録（stdio・アプリ起動不要）: `claude mcp add ayagaki -- /Applications/Ayagaki.app/Contents/MacOS/Ayagaki --mcp`
  / Codex: `codex mcp add ayagaki -- /Applications/Ayagaki.app/Contents/MacOS/Ayagaki --mcp`
- アプリ起動中は HTTP も可: `http://127.0.0.1:53536/mcp`
- ツール10個: list/get/create/update_design, paint, set_cells, get_notation, export_pdf, clear_cells, delete_design
- ⚠️ stdio で viewContext（main キュー + dispatchMain）に保存するとデッドロック → 専用バックグラウンドコンテキスト + DesignStore の performAndWait で解決済み。⚠️ SwiftUI ImageRenderer はヘッドレスで永久ハング → PDF は PDFSheetRenderer（CoreGraphics 直描き）

## 仕様（書籍と照合済みの規則）

- グリッド: 片面13目（60玉）/15目（68玉）× 段数。目盛りは **1=中央 … 13/15=外端**（綾書定規 4-10/4-11）
- **地の色に対して柄色は1つ**（ユーザー確認 2026-07-07）: セル値は 0=地・1=柄のみ。パレット UI は2色表示、MCP の入力検証も 0〜1（保存データの配列は互換のため4色のまま）
- 右半面は d=0 が中央線上で**右段が先に始まり**、左半面が1目(c)下がる配置で左右が隙間なく連続（左右1段ずれ。ユーザー確認 2026-07-07）
- 記号 = [糸交換の数字] + [綾名] + [⬆]：
  - **綾名**: 1目おきの「入れかえの目」（d=1,3,5…、60玉=6目）だけを読む。**全区間を「上a下b…」で数字付きで略さず書く**（1も書く。合計は常に6。例: 上1下5、下3上1下2。手書きの綾書の書式・ユーザー確認 2026-07-07）
  - **数字の書式**: 糸交換（数字）と戻し（丸数字）は1つの数列に数値順でまとめ、通常数字の後にだけ「.」（例: ②3.4.⑩⑪）。同種のみなら範囲圧縮（数字6個以上「2〜11.」、丸数字4個以上「②〜⑩」）
  - **戻し**: 戻し（丸数字）が要るのは**糸交換のみ**。上の操作（綾の手取りで目を上げる）には戻しを付けない（ユーザー確認 2026-07-07）。飛びの糸交換は**+2の番号**（2→④、3→⑤…）で模様の終わりの1段先にまとめて戻す。手取りの⬆のみ（中央の色なし）のブロックは書籍 4-15 の一括の戻し「②〜(M+2)」。数字は片面の目数で頭打ち、3個以下は並記・4個以上は範囲形式。**丸数字は終わった段の直後ではなく、ひとつ段を飛ばした先に表示**（次の模様と重なる場合はその記号の前に付く。回帰テストの 4-15 期待値もこの1段飛ばしで書いてある）。**中央の糸交換（⬆）の戻しは③**（毎回同じ番号）で、**反対の半面に出る**（左の⬆ → 右の記号列に③）。**中央の色から45度（1段1目でも、1段おきの2段2目でも続き扱い）に色が続く場合は、途中に糸交換を書かず、戻しは色が地色になる所の2段先**。番号は**終端の位置の番号+2（end.d+3）**: 中央単独=③、d2で終われば⑤、d4=⑦（進み方・色数によらず終端の位置で決まる。偶数列の糸交換の戻しも同じ式。ユーザー確認 2026-07-07）。**一番端（最外の目 d=12/14）まで達した連なりには戻しを書かない**（端で折り返すため。ユーザー確認 2026-07-07）
  - **縦に進む列（偶数列 d=2,4,…）は糸交換のみ**（綾名には入れない。ユーザー確認 2026-07-07）。番号は中央=1 からの通し（d+1: 3,5,7…13）。色のある段にその番号を書く。**戻しは、交換した対が45度で進んで色が地色になった後、終端から45°に進んだ「次の偶数列（交換できる目）」の段に、その位置の番号で行う**: 終端が偶数列なら2段先に番号+3、奇数列なら1段先に番号+2（例: d2止まり→2段先に⑤、d5止まり→1段先に⑦。端を越える場合は戻し不要。ユーザー確認 2026-07-07）。**45°の辿りは歩調を一貫させる**: 1段1目で進んできた連なりは空きを2段2目で飛び越えない（逆も同じ。歩調が変わる所で連なりは切れ、そこで戻す。ユーザー確認 2026-07-07）。**糸交換・戻しの番号は片面の目数（60玉=13）で頭打ち**
  - **斜めに色が続くときは色が変わるところだけ糸交換**（ユーザー確認 2026-07-07）: 前段の±1目から移ってきた色の目は交換に数えない。飛びの判定（2目以上増）も、縦列（偶数列）の毎段の数字も、斜めから繋がらない新しい目だけが対象
  - **糸交換から45度で続く色には、地色に戻る所まで糸交換を書かない**（ユーザー確認 2026-07-07）: 縞の一番先の交換も、遡って糸交換の目（中央、または fired の偶数列＝途中で発火した糸交換）に行き着く縞なら書かない（その区間は戻しが受け持つ。綾の段に糸交換番号は付けない。ユーザー確認 2026-07-07）。連なりの由来は追跡で判定（carried/fired）: **中央または発火した偶数列から続く色だけが省略対象**で、**綾（奇数列）から続いただけの偶数列には糸交換を書く**（例: d=11 の綾の次の段の d=12 →「13.」。ユーザー確認 2026-07-07）。**連なりの続き扱いは外向き（1目外・1段おきの2目外）のみ**で、内向きの斜めから続いた偶数列は糸交換を書く（そこから新しい連なりが始まり、終端で戻す。ユーザー確認 2026-07-07）
  - **斜めに進んだ縞は一番先で糸交換**（ユーザー確認 2026-07-07）: 入れかえの目を内側から外へ移ってきた色の縞は、一番先の目の色が終わる段に、その場所の番号（d+1）を1回だけ書く。戻しは不要。判定＝内側（d-2、d=1は中央の上ル）から移ってきて（直前の段または2段前）、外側（d+2）へは続かず（b+2段先まで見る）、かつ内側が縞の始まる前から終端まで塗られたままではない（＝広がる4-15型ではない）run。1段おきに塗る斜めにも対応
  - **飛び**: 入れかえの目が一度に2目以上増える段に「2.3.」等を前置。数字6個以上は「2〜M.」に範囲圧縮（書籍 4-16）。数字・丸数字と綾名の区切りはピリオド（ナミの前は区切りなし）
  - **⬆（中央で上ル）**: C 配列が唯一の情報源（index 2r=右半面・2r+1=左半面）。中央の2列（d=0）に色を置くと自動で**反対側の半面**に⬆。**左半面の⬆の表示は中央の色の1段前**（ユーザー確認 2026-07-07）。ただし**記号（戻し・縞・橋渡し）の法則は左右同一**で、上ルの効く段は左右とも中央の色と同じ段（ユーザー確認 2026-07-07: ずらすのは⬆の表示のみ）。連続 k 段の⬆が終わった段に ①〜ⓚ の戻し（置いた段に数字は出さない）。ナミ⬆の段は数えに中立。手動は記号表の左右セルをタップ
- 変更したら必ず `python3 scripts/test-notation.py` で 4-15 と一致することを確認

## 残タスク・未解決

- ⬆の完全自動導出と糸番号リスト（「3,5,7,9 上ル」等）: 糸1本ずつの経路追跡（二枚安田の組成シミュレーション）が必要。4-15 が答え合わせデータ
- 部分的な戻し（模様が全消えせず狭まる場合）の丸数字は未対応（現状は全消え段のみ）
- ~~色違い（柄1→柄2）の塗り替えは糸交換の数字に出ない~~ → 柄色を1つに制限したため解消（2026-07-07）

## 進め方の約束

- 決定は「書籍のページ写真 → ユーザー確認 → 実装 → 4-15 回帰テスト」の順
- Web（index.html）とアプリ（Shared/BraidModel.swift ほか）の**両方**に同じ変更を入れる
- デプロイ: push で GitHub Pages（Web）、/Applications へ ditto（アプリ）
